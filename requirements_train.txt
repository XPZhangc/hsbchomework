# 模型微调训练依赖

# 核心依赖
transformers>=4.30.0
torch>=2.0.0
datasets>=2.14.0
accelerate>=0.20.0

# LoRA/QLoRA微调
peft>=0.4.0

# QLoRA需要（可选）
bitsandbytes>=0.41.0

# 数据处理
pandas>=1.5.0
pyarrow>=10.0.0

# 其他工具
tensorboard>=2.13.0  # 可选，用于训练可视化
